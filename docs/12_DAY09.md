# Day 9 (4주차 목요일) - 로그 테이블 + 폴리싱

> **상태**: 🔲 예정  
> **목표**: AG-Grid 로그 테이블 완성 및 UI 폴리싱

---

## 📋 해야 할 일 (체크리스트)

- [ ] `components/log_table.py` 수정:
  - [ ] 이상 하이라이팅 강화 (파싱 실패, 체크섬 실패)
  - [ ] 로그 버퍼 제한 확인 (200개)
- [ ] `layouts/main_layout.py` 수정:
  - [ ] 스타일 폴리싱 (간격, 타이포그래피)
- [ ] 전체 UI 일관성 확인:
  - [ ] 색상 시맨틱 일관성
  - [ ] 간격/여백 통일
  - [ ] 폰트 크기 통일
- [ ] 장시간 실행 테스트 (30-60분)

---

## 📁 수정되는 파일 목록

```
ugv_mon/
├── components/
│   └── log_table.py  # 이상 하이라이팅 강화
├── layouts/
│   └── main_layout.py  # 스타일 폴리싱
└── utils/
    └── helpers.py  # 스타일 유틸리티 (필요 시)
```

---

## 💻 코드 구현 내용

### 1. `components/log_table.py` 수정

**주요 수정**: 파싱 실패/체크섬 실패 행의 시각적 강조

```python
def get_log_column_defs() -> List[Dict]:
    """컬럼 정의 수정 - 조건부 스타일링 강화"""
    return [
        # ... (기존 컬럼)
        {
            "headerName": "Parse",
            "field": "parse_ok",
            "width": 70,
            "cellStyle": {
                "styleConditions": [
                    {
                        "condition": "params.value === '✗'",
                        "style": {
                            "backgroundColor": "#fee2e2",
                            "color": "#991b1b",
                            "fontWeight": "600",
                        },
                    },
                    {
                        "condition": "params.value === '✓'",
                        "style": {"color": "#065f46"},
                    },
                ],
            },
        },
        {
            "headerName": "Checksum",
            "field": "checksum_ok",
            "width": 90,
            "cellStyle": {
                "styleConditions": [
                    {
                        "condition": "params.value === '✗'",
                        "style": {
                            "backgroundColor": "#fef3c7",
                            "color": "#92400e",
                            "fontWeight": "600",
                        },
                    },
                ],
            },
        },
        # Notes 컬럼 - 이상 감지 시 노란색 배경
        {
            "headerName": "Notes",
            "field": "notes",
            "width": 200,
            "flex": 2,
            "cellStyle": {
                "styleConditions": [
                    {
                        "condition": "params.value && params.value.length > 0",
                        "style": {
                            "backgroundColor": "#fef3c7",
                            "color": "#92400e",
                        },
                    },
                ],
            },
        },
    ]
```

---

### 2. UI 폴리싱

**주요 작업**:
- 간격/여백 통일 (24px, 16px, 8px 기준)
- 폰트 크기 통일 (14px 제목, 12px 본문)
- 색상 시맨틱 일관성 (성공=초록, 경고=노랑, 오류=빨강)
- 카드 그림자/테두리 통일

---

## 🧪 테스트 방법

### 장시간 실행 테스트

```bash
# 30-60분 실행
sudo python run.py

# 확인 사항:
# - 로그가 200개로 제한되는지
# - 메모리 사용량이 안정적인지
# - UI 반응 속도가 유지되는지
```

### 브라우저 개발자 도구

- **Console**: JavaScript 오류 없음
- **Performance**: 메모리 누수 없음
- **Network**: 불필요한 요청 없음

---

## ✅ 결과물 확인

### 성공 기준

1. ✅ 로그 테이블이 200개로 제한됨
2. ✅ 이상 로그가 시각적으로 구분됨
3. ✅ 전체 UI 스타일이 일관됨
4. ✅ 30-60분 실행 시 메모리 안정적

---

## 📝 커밋 메시지

```
feat(logs): finalize ag-grid log view and UI polish for monitoring workflow

- Add conditional styling for parse/checksum failures
- Implement bounded log buffer (200 entries)
- Polish UI spacing, typography, and color consistency
- Enhance anomaly highlighting in log table
- Ensure enterprise dashboard look and feel
- Optimize performance for long-running sessions

Implements CSC-003: Data Presentation (로그/이벤트 전시)
```

---

## 📚 다음 단계

**Day 10**: 통합 안정화 + 문서화
- E2E 테스트
- 단위 테스트 작성
- 문서 최종 정리
